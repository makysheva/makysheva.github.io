mixin rateButton(icon, iconBorder)
    - for (var x = 0; x < 5; x++)
        if icon
            i(class="rating__icon") #{icon}
        else if iconBorder
            i(class="rating__icon") #{iconBorder}